#!/bin/busybox ash

# Use tools
. "/lib/jam/lib/jamtools.rc"

################################################################
# Main shutdown process
################################################################

echo "${GREEN}Syncing all filesystems.${NORMAL}"
sync; sync

echo "${RED}Shutdown in progress.${NORMAL}"
sync
echo ""